project.name = course

[id]
type = id

[user_id]
type = string
index = self
tokenizer = full

[category_id]
type = string
index = self
tokenizer = full

[type]
type = string
index = self
tokenizer = full

[title]
type = title

[summary]
type = body

[tags]
type = string
index = both

[rating]
type = numeric

[score]
type = numeric

[price]
type = numeric

[level]
type = string
index = self
tokenizer = full

[status]
type = string
index = self
tokenizer = full

[create_time]
type = numeric
